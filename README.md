# How to set PATH to use `mkcs` & `opcs` commands

1. Add following contents to `.bash_profile`

```
export PATH="$HOME/handler/bin:$PATH"
alias mkcs='mkcase.sh'
alias opcs='opcase.sh'
```

2. Run the commnads; `$ source ~/.bash_profile`


# How to use
